# Security Configuration for DEASemester Directory
# This file prevents direct browser access to exam questions

# Disable directory listing
Options -Indexes

# Deny all direct access
<RequireAll>
    Require all denied
</RequireAll>

# Block common file types from direct access
<FilesMatch "\.(pdf|doc|docx|txt|zip|rar)$">
    <RequireAll>
        Require all denied
    </RequireAll>
</FilesMatch>

# Prevent access to hidden files
<FilesMatch "^\.">
    <RequireAll>
        Require all denied
    </RequireAll>
</FilesMatch>

# Security headers
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options DENY
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>
